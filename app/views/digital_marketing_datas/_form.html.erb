<%= form_for(digital_marketing_data, url: path, method: method) do |f| %>
  <% if digital_marketing_data.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(digital_marketing_data.errors.count, "error") %> prohibited this digital_marketing_data from being saved:</h2>
      <ul>
      <% digital_marketing_data.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :company %>
    <%= f.select :company_id, options_from_collection_for_select(Company.all, :id, :name, digital_marketing_data.company_id), class: 'form-control' %>
    <br /><br />
  </div>
  
  <div class="field">
    <%= f.label :date %>
    <%= f.date_field :date, class: 'form-control' %>
    <br />
  </div>

  <div class="field">
    <%= f.label :amount %>
    <%= f.number_field :amount, class: 'form-control' %>
    <br />
  </div>

  <div class="field">
    <%= f.label :search_engine %>
    <%= f.select :search_engine, options_for_select(['google', 'bing', 'yahoo'], @digital_marketing_data.search_engine), class: 'form-control' %>
    <br /><br />
  </div>
  
  <div class="actions">
     <%= f.submit 'Submit', class: 'btn btn-success' %>
  </div>
<% end %>
